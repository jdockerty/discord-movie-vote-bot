version: "3.8"

services:
  bot:
    build:
      context: .
      dockerfile: .docker/bot.yml
    ports:
      - "9091"
      
  prometheus:
    image: prom/prometheus
    volumes:
      - ".docker/prometheus.yml:/etc/prometheus/prometheus.yml"
    ports:
      - "9090"

